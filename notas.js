/*

git init
git branch -m main
git status
git add .
git commit -m ''
git config --global alias.tree 'instruc'
git diff -> 
git checkout archivo.* -> Me devuelve el archivo al ultimo commit guardado
git reset -> descarta cambios y me regresa al ultimo commit hecho
git log -> retorna registros del inicio al commit que esta en el HEAD
git reset --hard <id> -> me regresa atras o adelante el head al commit especificado
git reflog -> retorna registros de todo el proyecto 
git tag nombredelTag -> etiquetar commits importantes por ej versiones; git tag clase_1
git tag -> tambien lista todos los tags creados
git revert -> eliminar commit completo (avanzado)
git branch nuevaRama
git switch ramaAMoverse
git stash -> guarda los cambios de mi repo local de manera temporal antes de hacer un commit
git stash list -> lista los cambios que no he guardado en un commit
*/

console.log('Notasss');